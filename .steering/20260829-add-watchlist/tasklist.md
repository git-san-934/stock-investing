# タスクリスト: ウォッチリスト機能の追加

## 実装タスク
- [ ] `stock_signals.py`: `parse_watchlist_codes(raw_text, max_codes=10)` を追加(改行・カンマ区切りに対応、トリム・重複除去・上限チェック)
- [ ] `stock_signals.py`: シグナルレベルの並び順定数(`SIGNAL_LEVEL_ORDER`)を追加
- [ ] `app.py`: サイドバーの入力欄をテキストエリア(複数コード入力)に変更
- [ ] `app.py`: 入力上限(10件)超過時のエラーメッセージ表示を実装
- [ ] `app.py`: `st.cache_data` でラップしたデータ取得ヘルパーを追加
- [ ] `app.py`: 銘柄ごとのループ処理(取得成功→サマリー集計、失敗→エラーリストに集計)を実装
- [ ] `app.py`: 一覧テーブル(シグナルの強さ順にソート)の表示を実装
- [ ] `app.py`: 取得失敗銘柄のエラー表示を実装
- [ ] `app.py`: 銘柄選択用 `st.selectbox` を実装(初期選択=一覧の先頭)
- [ ] `app.py`: 選択銘柄に対する既存の詳細チャート描画(ローソク足・移動平均・売買代金・データテーブル)を接続
- [ ] `README.md`: 複数コード入力に対応した使い方説明に更新

## 進捗状況
未着手(このタスクリストの承認後に着手する)

## 完了条件
* `.steering/20260829-add-watchlist/requirements.md` に記載した受け入れ条件をすべて満たす
* `python -m py_compile app.py stock_signals.py` が成功する(構文エラーなし)
* ネットワークが利用可能な環境で、複数の証券コード(例: `7203, 9984, 6758`)を入力し、一覧表示・詳細表示・エラーハンドリング(存在しないコードを含めた場合)が意図通り動作することを確認する
* 変更内容をコミットし、`git-san-934/stock-investing` の `main` ブランチにpushする
